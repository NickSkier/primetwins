# Primetwins

Just a repo where I try different programming models for heterogeneous systems using slow prime twins finding algorithm.

# CUDA

## Build
```bash
nvcc -o run main.cu -O3
````

## Run
```bash
./run 0 10000
````
